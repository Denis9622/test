{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "scss": "sass --watch ./src/scss/styles.scss:./src/css/styles.css",
    "build": "sass ./src/scss/styles.scss:./dist/css/styles.css && cp -r ./src/* ./dist",
    "deploy": "gh-pages -d dist"
  },
  "keywords": [],
  "author": "Denys Zinchenko",
  "license": "ISC",
  "dependencies": {
    "swiper": "^11.2.2"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "sass": "^1.84.0"
  }
}
